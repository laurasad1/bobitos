let diomio;


diomio = prompt("pon una letra o una vaina ahi")
   
    
    if (diomio == 'a' || diomio == 'e' || diomio == 'i' || diomio == 'o' || diomio == 'u') {
    
        document.write("Eso es una vocal manito");
    
    }
    
    else {
        document.write("Eso es otro caracter pp");
    }