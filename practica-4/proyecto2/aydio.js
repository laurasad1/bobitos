let cuadradox = 0;

cuadradox = prompt("El lado del cuadradox es:");

function perimetroflow(Lados) {
    Lados = Lados *4;
    document.write(" El perimetro del cuadradox es: " + La2);
}

function superficieflow(La2) {
    La2 = La2 * La2;
    document.write(" La superficie del cuadradox es: " + La2);
}

perimetroflow(cuadradox);

superficieflow(cuadradox);